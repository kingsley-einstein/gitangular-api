type User {
    id: ID!
    email: String!
    username: String!
    password: String!
    github: String!
    token: String,
    location: Location
    picture: Picture
}

type Query {
    findById(id: ID!): User!
    findByToken(token: String!): User!
    log(email: String!, password: String!): User!
}

type Mutation {
    create(email: String!, username: String!, password: String!, github: String!): User!
    update(email: String!, password: String!): User!
    delete(id: ID!): Boolean!
}